# INSTRUCTIONS IN ENGLISH ------ GOREVLENDIRME TURKCE
| English Instructions | Türkçe Talimatlar |
| :--- | :--- |
| ***### Difficulty Degree:*** 8.5/10 | ***### Zorluk duzeyi:*** 8.5/10|
| SCENARIO: As a data analyst you receive raw data “pipe separated” (separated by \|) from a cybersecurity device. However, your analysis tool only accepts this data as a key-value (key-value) pair. This plain text of yours, awk just without using it sed you need to put it in a format configured with | SENARYO: Bir veri analisti olarak size bir siber güvenlik cihazından "pipe separated" ( \| ile ayrılmış) ham veriler geliyor. Ancak analiz aracınız bu veriyi sadece anahtar-değer (key-value) çifti şeklinde kabul ediyor. Sizin bu düz metni, awk kullanmadan sadece sed ile yapılandırılmış bir formata sokmanız gerekiyor. |
| **Step 1:** Prepare the data (or download from here).  | **Adım 1:** Data Olustur (veya buradan indir).  |
| Golden Information: | Altin Bilgi: |
| By opening parentheses within parentheses in Regex, you can capture both all and part of the data at the same time.
Ex: (([0-9]{2})\/([0-9]{2}))
Here \1 while giving the entire date (day/month), \2 just the day \3 only the bear gives. This gives you tremendous flexibility when creating structured data like JSON. | Regex'te parantez içinde parantez açarak verinin hem tamamını hem de bir parçasını aynı anda yakalayabilirsiniz. <br><br> Örn: (([0-9]{2})\/([0-9]{2})) <br> Burada \1 tüm tarihi (gün/ay) verirken, \2 sadece günü, \3 sadece ayı verir. Bu, JSON gibi yapılandırılmış veri oluştururken size muazzam bir esneklik sağlar. |
| Analytical Question: | Analitik Soru: |
| When creating a hierarchical format like JSON, sedWhat restrictions does the line-based (line-by-line) working principle of impose on us? For example, a JSON object spread over multiple lines sed why manage with a "normal" substitution (s///Much more difficult than ) operation? <br><br> Sir, in this task, especially for that great transformation in Article 1 \1, \2, \3... I expect you to push its use to the extreme. Good luck! | JSON gibi hiyerarşik bir format oluştururken, sed'in satır bazlı (line-by-line) çalışma prensibi bize ne gibi bir kısıtlama getirir? Örneğin birden fazla satıra yayılmış bir JSON objesini sed ile yönetmek neden "normal" bir substitution (s///) işleminden çok daha zordur? |
| sed's being line-based, like JSON block (multiline) it challenges us in structures. Because sed when he finishes one line and moves on to the next, he forgets the old line. If a JSON object is spread over 5 lines, sedIt is necessary to use the "Hold Space" (backup memory) feature of, which makes the commands very complicated. For this reason sed it is generally preferred for single-line (inline) JSONs | sed'in satır bazlı olması, JSON gibi blok (multiline) yapılarda bizi zorlar. Çünkü sed bir satırı bitirip diğerine geçtiğinde eski satırı unutur. Eğer bir JSON objesi 5 satıra yayılmışsa, sed'in "Hold Space" (yedek bellek) özelliğini kullanmak gerekir ki bu da komutları çok karmaşıklaştırır. Bu yüzden sed genellikle tek satırlık (inline) JSON'lar için tercih edilir |
| **Step 2:** MISSION | **Adım 2:** GOREV |
| 1. Dynamic Labeling: Convert each line to: {"timestamp": "TARİH", "user": "KULLANICI", "event": "OLAY", "risk_score": "SKOR"}. <br><br> 2. Score Normalization: Risk scores 0-100 coming between. What is asked of you, 9[0-9] to the end of all scores (between 90-99) _CRITICAL phrase, sedRecall of (\nyou add it with the ) feature. <br><br> 3. Unnecessary Data Pruning: Technical error codes at the end of the line (Ex: ERR_CODE: 404) delete completely, do not include in JSON format. <br><br> 4. Date Masking: The day and month should appear in the analysis report, but the year part is for security reasons XXXX it should be changed to (Ex: 12/03/2026 -> 12/03/XXXX). <br><br> 5. Automation: This complex transformation is a single one sed do it with chain of command. | 1. Dinamik Etiketleme: Her satırı şu formata çevirin: {"timestamp": "TARİH", "user": "KULLANICI", "event": "OLAY", "risk_score": "SKOR"}. <br><br> 2. Skor Normalizasyonu: Risk skorları 0-100 arasında geliyor. Sizden istenen, 9[0-9] (90-99 arası) olan tüm skorların sonuna _CRITICAL ibaresini, sed'in geri çağırma (\n) özelliği ile eklemeniz. <br><br> 3. Gereksiz Veri Budama: Satır sonundaki teknik hata kodlarını (Örn: ERR_CODE: 404) tamamen silin, JSON formatına dahil etmeyin. <br><br> 4. Tarih Maskeleme: Analiz raporunda gün ve ay görünmeli ama yıl kısmı güvenlik gereği XXXX olarak değiştirilmeli (Örn: 12/03/2026 -> 12/03/XXXX). <br><br> 5. Otomasyon: Bu karmaşık dönüşümü tek bir sed komut zinciri ile yapın. |
| **Step 3:* SOLUTION | **Adım 3:** CEVAP |

``` bash
sed -E 's#^([0-9]{2}/[0-9]{2})/[0-9]{4}[[:space:]]\|[[:space:]]\
([^[:space:]]+)[[:space:]]\|[[:space:]]([^[:space:]]+)\
[[:space:]]\|[[:space:]]([0-9]+)[[:space:]]\|[[:space:]]ERR_CODE:[[:space:]]([0-9]{3})$\
#{"timestamp": "\1/XXXX", "user": "\2", "event": "\3", "risk_score": "\4", "err_code": "\5"}#'\
g26_raw_traffic.log
```
